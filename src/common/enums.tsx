export enum ContactTab {
  MY_CONTACTS,
  ADD_NEW,
}

export enum FeedbackAction {
  LIKE = "like",
  DISLIKE = "dislike",
  NONE = "none",
}

export enum SignInTab {
  SIGN_IN,
  SIGN_UP,
}

export enum Theme {
  LIGHT = "light",
  DARK = "dark",
}
