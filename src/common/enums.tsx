export enum ConnectionsTab {
  CONNECTIONS,
  ADD_CONNECTIONS,
}

export enum FeedbackAction {
  LIKE = "like",
  DISLIKE = "dislike",
  NONE = "none",
}

export enum SignInTab {
  SIGN_IN,
  SIGN_UP,
}

export enum Theme {
  LIGHT = "light",
  DARK = "dark",
}
